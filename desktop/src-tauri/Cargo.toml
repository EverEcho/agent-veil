[package]
name = "agentveil-desktop"
version = "0.1.0"
description = "AgentVeil local privacy desktop control plane"
authors = ["AgentVeil contributors"]
edition = "2021"
rust-version = "1.88.0"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
base64 = "0.22"
rand = "0.9"
reqwest = { version = "0.12", default-features = false, features = ["blocking", "json", "rustls-tls"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-autostart = "2"
tauri-plugin-single-instance = "2"
tauri-plugin-updater = "2.11.0"
url = "2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true

# Dev packages favor turnaround time. Beta and release continue to use the fully
# optimized release profile above.
[profile.dev-package]
inherits = "release"
codegen-units = 8
lto = "thin"
opt-level = 2
